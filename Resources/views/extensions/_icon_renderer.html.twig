<svg width="{{ width }}" height="{{ height }}" {% if class %}class="{{ class }}"{% endif %}>
    <use href="{{ "#{qag().config.assets_path}/tabler-sprite.svg#tabler-#{name}" }}" xlink:href="{{ "#{qag().config.assets_path}/tabler-sprite.svg#tabler-#{name}" }}" />
</svg>